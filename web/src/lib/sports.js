/**
 * Display names for the sports the schema knows about.
 *
 * Separated from `api/fixtures.js`, where this used to live: it is interface
 * vocabulary rather than sample data, and it has to outlive the fixtures.
 * The keys match `domain.Sport`.
 */
export const SPORT_LABELS = {
	futsal: 'Futsal',
	basketball: 'Basketball',
	badminton: 'Badminton',
	cricket_net: 'Cricket net',
	tennis: 'Tennis'
};
